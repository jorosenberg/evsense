# Deprecated scrapers, retained for opt-in use only. See README.md.
# main.py imports these lazily as `archive.<module>` (falling back to
# `scrapers.<module>` if you have not physically moved the files yet).
