/**
 * REMOVED — Gas savings comparison was intentionally removed from EVsense per
 * scope clarification on 2026-05-27. Monthly charging cost in the TCO is the
 * canonical view; gas vs. EV side-by-side comparison is no longer in scope.
 *
 * This stub remains so any straggling imports surface as obvious build errors
 * rather than silently rendering empty UI. Delete this file once you have
 * confirmed nothing in the tree imports it.
 */
export default function GasComparisonWidget() {
  return null
}
